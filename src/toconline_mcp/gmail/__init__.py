"""Gmail integration — search messages and download attachments.

Lives alongside the TOCOnline integration so a single MCP server can bridge
both systems (e.g. download supplier invoice PDFs from email, create matching
purchase documents in TOCOnline).
"""
